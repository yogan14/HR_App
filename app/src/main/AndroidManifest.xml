<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hr_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.storage.ListPicActivity"/>
        <activity android:name=".ui.storage.StorageActivity">
        </activity>
        <activity android:name=".ui.MyAbsencesActivity" />
        <activity android:name=".ui.BaseHRActivity" />
        <activity android:name=".ui.ModifyRequestAbsenceActivity" />
        <activity android:name=".ui.ModifyPersonActivity" />
        <activity android:name=".ui.AddPersonActivity" />
        <activity android:name=".ui.RequestAbsencesActivity" />
        <activity android:name=".ui.CollaboratorsActivity" />
        <activity android:name=".ui.mgmt.SettingsActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.mgmt.LoginActivity">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>